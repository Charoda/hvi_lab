# Скопируйте в .env и подставьте свой ключ (файл .env в git не попадает):
#   cp .env.example .env && chmod 600 .env
#
# OpenRouter — для --actor minimax (MiniMax M3 free):
OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
